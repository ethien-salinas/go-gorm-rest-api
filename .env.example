# Copia este archivo a .env y completa los valores reales
# cp .env.example .env

# Base de datos
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=tu_password_aqui
DB_NAME=mydb
DB_PORT=5432
DB_SSLMODE=disable

# Servidor
PORT=3000

# Migraciones (solo activar en desarrollo)
AUTO_MIGRATE=true
